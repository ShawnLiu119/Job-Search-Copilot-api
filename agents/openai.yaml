interface:
  display_name: "Job Search Copilot"
  short_description: "Match jobs, tailor resumes, and prepare interviews"
  default_prompt: "Use $job-search-copilot to find suitable roles, create a clean one-page tailored resume for each shortlisted job, prepare networking outreach, and draft five role-specific mock interview questions."
